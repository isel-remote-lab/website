import ManageHardwareModal from "../(.)labs/[id]/settings/hardware/ManageHardwareModal";

export default async function ManageAllHardwareModal() {
  return (
    <div>
      <ManageHardwareModal/>
    </div>
  );
}
